import { Typography } from '@mui/material';
import React from 'react';

function WebTech({}) {
  return (
    <Typography
      noWrap
      component="div"
      sx={{ color: 'black' }}
      fontFamily={'Quick'}
      fontSize={30}
    >
      WebTech
    </Typography>
  );
}

export default WebTech;
